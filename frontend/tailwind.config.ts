import type { Config } from 'tailwindcss';

const config: Config = {
  darkMode: ['class'],
  content: [
    './pages/**/*.{ts,tsx}',
    './components/**/*.{ts,tsx}',
    './app/**/*.{ts,tsx}',
    './src/**/*.{ts,tsx}',
  ],
  theme: {
    container: {
      center: true,
      padding: '2rem',
      screens: {
        '2xl': '1400px',
      },
    },
    extend: {
      colors: {
        'auth-bg': 'hsl(var(--auth-bg))',
        'auth-card': 'hsl(var(--auth-card))',
        'auth-heading': 'hsl(var(--auth-heading))',
        'auth-body': 'hsl(var(--auth-body))',
        'cohold-blue': 'hsl(var(--cohold-blue))',
        'cohold-icon-bg': 'hsl(var(--cohold-icon-bg))',
        'cohold-link': 'hsl(var(--cohold-link))',
        'dashboard-bg': 'hsl(var(--dashboard-bg))',
        'dashboard-card': 'hsl(var(--dashboard-card))',
        'dashboard-heading': 'hsl(var(--dashboard-heading))',
        'dashboard-body': 'hsl(var(--dashboard-body))',
        'dashboard-muted': 'hsl(var(--dashboard-muted))',
        'dashboard-border': 'hsl(var(--dashboard-border))',
        'dashboard-nav-active': 'hsl(var(--dashboard-nav-active))',
        border: 'hsl(var(--border))',
        input: 'hsl(var(--input))',
        ring: 'hsl(var(--ring))',
        background: 'hsl(var(--background))',
        foreground: 'hsl(var(--foreground))',
        primary: {
          DEFAULT: 'hsl(var(--primary))',
          foreground: 'hsl(var(--primary-foreground))',
        },
        secondary: {
          DEFAULT: 'hsl(var(--secondary))',
          foreground: 'hsl(var(--secondary-foreground))',
        },
        destructive: {
          DEFAULT: 'hsl(var(--destructive))',
          foreground: 'hsl(var(--destructive-foreground))',
        },
        muted: {
          DEFAULT: 'hsl(var(--muted))',
          foreground: 'hsl(var(--muted-foreground))',
        },
        accent: {
          DEFAULT: 'hsl(var(--accent))',
          foreground: 'hsl(var(--accent-foreground))',
        },
        popover: {
          DEFAULT: 'hsl(var(--popover))',
          foreground: 'hsl(var(--popover-foreground))',
        },
        card: {
          DEFAULT: 'hsl(var(--card))',
          foreground: 'hsl(var(--card-foreground))',
        },
      },
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)',
        'auth-radius': 'var(--auth-radius)',
        'auth-radius-lg': 'var(--auth-radius-lg)',
      },
      keyframes: {
        'accordion-down': {
          from: { height: '0' },
          to: { height: 'var(--radix-accordion-content-height)' },
        },
        'accordion-up': {
          from: { height: 'var(--radix-accordion-content-height)' },
          to: { height: '0' },
        },
      },
      animation: {
        'accordion-down': 'accordion-down 0.2s ease-out',
        'accordion-up': 'accordion-up 0.2s ease-out',
      },
    },
  },
  plugins: [require('tailwindcss-animate')],
};

export default config;
